# Simple Python-Rust interagrion testing

### Possible bugs
- Because of the way that I handle the strings, the buffer has a fixed size which could be problematic
- The price HAS TO BE an integer. The library used for localizing the price does not suport floating pointing numbers
